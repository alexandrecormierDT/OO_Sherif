
$("#root_folder_form").submit(function(e){ // On sélectionne le formulaire par son identifiant
    e.preventDefault(); // Le navigateur ne peut pas envoyer le formulaire

	window.Sherif.control.send_root_folder_form();

});










				
